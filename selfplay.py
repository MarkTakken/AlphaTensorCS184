## Self-play is meant to control the self-play loop
from architecture import *
from mcts import *
from utilities import *

def self_play(model):
    model.eval()

    # Build a set of target tensors

    # Play the game using MCTS and model for each tensor
    
    # Save the trajctories to a file

    # If any find the zero tensor at the end, it is a algorithm that works

    # Un-change-of-basis them, return those, save them someone special